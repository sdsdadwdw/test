实现一个单机版的对象存储服务，提供对象的PUT和GET方法。当客户端PUT方法访问服务时，客户端提供对象的名字和内容
我们的服务将对象的内容以文件的形式存储在服务器的本地磁盘上，当客户端使用GET方法访问时，
再从本地此版读取文件内容并将其作为HTTP的响应输出

重点代码：
    serve.go（主函数）
    handler.go(处理函数)
    put.go(put方法函数)
    get.go(get方法函数)
在Ubuntu16.04 上使用curl模拟http命令请求